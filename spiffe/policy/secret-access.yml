# Give the SPIFFE workloads group read access to the demo secret the
# report-service fetches.
# Load into branch: data/demo-apps
#   conjur policy load -b data/demo-apps -f policy/secret-access.yml
- !permit
  role: !group /data/spiffe-apps
  privileges: [read, execute]
  resource: !variable app-db/password
